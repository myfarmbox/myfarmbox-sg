window.MFB_CONFIG = {
  // Paste your deployed Apps Script Web App URL here:
  API_URL: "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL",

  // Optional. If blank, the app still works as a list.
  GOOGLE_MAPS_API_KEY: "",

  // Driver identity for V1.
  // Later this can come from login.
  DRIVER_ID: "SG-DRIVER-01",
  DRIVER_NAME: "Delivery Team",

  // Singapore default map center.
  DEFAULT_CENTER: { lat: 1.3521, lng: 103.8198 },

  // Refresh delivery status every 15 seconds in Live mode.
  LIVE_REFRESH_MS: 15000
};
