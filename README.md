# MyFarmBox.sg Coming Soon

## Upload to GitHub
1. Create a public repository, for example `myfarmbox-sg`.
2. Upload all files from this folder to the repository root.
3. Open **Settings → Pages**.
4. Under **Build and deployment**, select **Deploy from a branch**.
5. Select branch `main` and folder `/(root)`, then save.
6. In **Custom domain**, enter `myfarmbox.sg`.

## Important
The early-access form currently uses FormSubmit and sends to:
`hello@myfarmbox.sg`

The first submission may require email activation. Replace the form action if you prefer Google Apps Script later.
